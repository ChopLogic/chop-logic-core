import {
	HilbertCalculus,
	NaturalCalculus,
	PropositionalFactory,
	PropositionalUtils,
} from "./propositional";
import { Tokenizer } from "./tokenizer";

export const ChopLogicCore = Object.freeze({
	Tokenizer,
	PropositionalFactory,
	PropositionalUtils,
	HilbertCalculus,
	NaturalCalculus,
});
